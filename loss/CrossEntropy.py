#! -*-coding:utf8-*-

import numpy as np 

def CrossEntropyLoss(y, labels):
    """交叉熵损失
    input:
      y: 预测值
      labels: 正确的label
    output:
      输出loss值
    """
    pass

def gradient_CrossEntropyLoss(gradient_last, input, output):
    """交叉熵梯度
    input:
      granditent_lash: 后一层的梯度
      input: 这一层的输入
      output: 这一层的输出
    output:
      gw: 这一层w的梯度
      gb: 这一层b的梯度
    """
    pass
